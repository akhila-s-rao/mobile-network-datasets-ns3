/*  // Get CellId for that UE from rrc ? I need to pass the params and UE node container here
  if (info.simulator == "5GLENA"){
      Ptr<NrUeNetDevice> ueNetDev = ue_node->GetDevice (0)->GetObject<NrUeNetDevice> ();
      //rnti = ueNetDev->GetRrc ()->GetRnti ();
      imsi = ueNetDev->GetImsi ();
      cellId = info.scenario->GetCellIndex (ueId);
   }
  else if (info.simulator == "LENA"){
      Ptr<LteUeNetDevice> ueNetDev = ue_node->GetDevice (0)->GetObject<LteUeNetDevice> ();
      //rnti = ueNetDev->GetRrc ()->GetRnti ();
      imsi = ueNetDev->GetImsi ();
      cellId = info.scenario->GetCellIndex (ueId);
   }*/

